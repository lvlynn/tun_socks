#include <nt_def.h>
#include <nt_core.h>

int main()
{
    nt_cycle_t * cycle;
    //cycle =( nt_cycle_t * ) malloc( sizeof( nt_cycle_t   )   );
    cycle =( nt_cycle_t * ) malloc( sizeof( int  )   );
    printf("Hello world\n");
    return 0;
}

